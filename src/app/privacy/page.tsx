import type { Metadata } from "next";
import Link from "next/link";
import { Icon } from "@/lib/icons";

export const metadata: Metadata = {
  title: "Политика конфиденциальности — Dots",
  description: "Какие данные хранит Dots, где и кто их видит",
};

/** Когда текст последний раз менялся по существу. */
const UPDATED = "25 сентября 2026 года";

/**
 * Политика конфиденциальности.
 *
 * Написана по тому, что приложение делает на самом деле, а не по шаблону:
 * меняется устройство данных — меняется и этот текст, вместе с датой.
 * Открыта без входа: её читают до регистрации.
 */
export default function PrivacyPage() {
  const contact = process.env.NEXT_PUBLIC_CONTACT?.trim();

  return (
    <div className="pt-safe mx-auto w-full max-w-md px-5 pb-16">
      <header className="flex items-center gap-2 py-3">
        <Link
          href="/"
          aria-label="На главную"
          className="-ml-2 flex h-9 w-9 items-center justify-center rounded-full"
          style={{ color: "var(--muted)" }}
        >
          <Icon name="chevron-left" size={20} />
        </Link>
        <h1 className="text-[1.375rem] font-semibold leading-tight">Политика конфиденциальности</h1>
      </header>
      <p className="text-[0.75rem]" style={{ color: "var(--muted)" }}>
        Обновлена {UPDATED}
      </p>

      <Section title="Коротко">
        <p>
          Dots — это «Деньги» и «Задачи» под одной учётной записью. Ваши записи видите только
          вы. Мы не показываем рекламу, не продаём и не передаём данные для рекламы, не ставим
          счётчики посещений и трекеры.
        </p>
      </Section>

      <Section title="Что мы храним">
        <ul>
          <li>Адрес почты и пароль для входа (пароль — только в виде хеша, прочитать его нельзя).</li>
          <li>
            Если входите через Google — имя, адрес почты и картинку профиля, которые передаёт
            Google. Картинка профиля становится вашей аватаркой в приложении.
          </li>
          <li>
            Имя, которое вы указали при регистрации или в настройках: по нему приложение к вам
            обращается.
          </li>
          <li>Настройки: основную валюту, курсы валют, тему, размер шрифта.</li>
          <li>
            Деньги: кошельки, накопления, долги, категории, операции с суммами, датами и
            заметками.
          </li>
          <li>Задачи: их текст, сроки и отметки о выполнении.</li>
          <li>Номер учётной записи (ID) — он выдаётся по порядку регистрации.</li>
          <li>
            Когда вы последний раз открывали приложение: пока оно открыто на экране, раз в минуту
            отмечается время — чтобы было видно, сколько людей пользуются им сейчас.
          </li>
          <li>Ключ OpenRouter, если вы его добавили, и сколько раз вы запускали ИИ-разбор.</li>
          <li>Ваши обращения к администратору и его ответы.</li>
        </ul>
      </Section>

      <Section title="Где хранятся данные">
        <p>
          В базе данных Supabase, сервера во Франкфурте (Евросоюз). Само приложение работает на
          Vercel. Между телефоном и серверами данные идут только по защищённому соединению
          (HTTPS). Правила в базе пускают каждого только к его собственным записям.
        </p>
      </Section>

      <Section title="Кто видит ваши данные">
        <p>Ваши суммы, операции, заметки и задачи видите только вы.</p>
        <p>
          У администратора есть панель со сводкой по пользователям: почта, имя, ID, почта
          и картинка профиля привязанного Google, дата регистрации, когда вы были в приложении последний раз, и
          счётчики — сколько операций, кошельков и задач, добавлен ли ключ ИИ и сколько раз им
          пользовались. Сумм, названий операций и текстов задач в этой панели нет. Ещё
          администратор видит обращения, которые вы ему написали.
        </p>
        <p>
          Как у любого сервиса, у его владельца есть технический доступ к базе. Он нужен, чтобы
          чинить сбои и восстанавливать данные, а не чтобы читать ваши записи.
        </p>
      </Section>

      <Section title="ИИ-разбор бюджета">
        <p>
          Работает только по нажатию и только с вашим личным ключом OpenRouter. Тогда в
          OpenRouter уходит сводка за месяц: суммы по категориям и подкатегориям, лимиты,
          названия и остатки кошельков, долги и накопления. Отдельные операции и заметки к ним
          не отправляются. Дальше сводку обрабатывают OpenRouter и выбранная вами модель — по
          своим правилам.
        </p>
        <p>
          Ключ хранится в базе и в браузер не возвращается: разбор запрашивает сервер
          приложения.
        </p>
      </Section>

      <Section title="Выписка из банка">
        <p>
          PDF-выписку приложение читает прямо на вашем телефоне: файл никуда не отправляется.
          Сохраняются только те операции, которые вы подтвердили.
        </p>
      </Section>

      <Section title="Что остаётся на телефоне">
        <p>
          Данные входа, чтобы не входить каждый раз. Тема, размер шрифта и то, скрыты ли суммы
          «глазком», — чтобы приложение сразу открывалось в нужном виде. Рекламных и
          аналитических cookie нет.
        </p>
        <p>
          Картинку профиля Google телефон загружает прямо с серверов Google — так же, как её
          показывают другие сервисы. Адрес нашей страницы при этом Google не передаётся.
        </p>
      </Section>

      <Section title="Выгрузка и удаление">
        <p>
          Операции можно выгрузить в CSV: «Деньги» → «Настройки» → «Выгрузить в CSV».
        </p>
        <p>
          Чтобы удалить учётную запись вместе со всеми данными, напишите администратору:
          «Настройки» → «Написать администратору». Удаляется всё: операции, кошельки, задачи,
          ключ ИИ, обращения и сама учётная запись.
        </p>
      </Section>

      <Section title="Изменения">
        <p>
          Если мы начнём хранить или передавать что-то новое, этот текст изменится, а дата
          вверху обновится.
        </p>
      </Section>

      <Section title="Связаться">
        {contact ? (
          <p>
            По вопросам о данных:{" "}
            <a
              href={contact.includes("@") && !contact.startsWith("@") ? `mailto:${contact}` : undefined}
              className="font-medium"
              style={{ color: "var(--accent)" }}
            >
              {contact}
            </a>
          </p>
        ) : (
          <p>
            В приложении: «Настройки» → «Написать администратору». Ответ придёт туда же.
          </p>
        )}
      </Section>
    </div>
  );
}

function Section({ title, children }: { title: string; children: React.ReactNode }) {
  return (
    <section className="mt-6">
      <h2 className="mb-2 text-[1rem] font-semibold">{title}</h2>
      <div className="selectable space-y-2 text-[0.875rem] leading-relaxed [&_li]:relative [&_li]:pl-4 [&_li]:before:absolute [&_li]:before:left-0 [&_li]:before:content-['•'] [&_li]:before:text-[var(--muted)] [&_ul]:space-y-1.5">
        {children}
      </div>
    </section>
  );
}
