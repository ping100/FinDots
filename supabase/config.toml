# Минимальный конфиг для интеграции Supabase с GitHub.
#
# Файл нарочно короткий: Supabase умеет применять настройки проекта прямо
# отсюда, и любая лишняя строка тихо перебила бы то, что выставлено руками
# в панели — например, включённый вход по почте. Здесь только имя проекта,
# всё остальное остаётся за панелью.
project_id = "dotsapp"
