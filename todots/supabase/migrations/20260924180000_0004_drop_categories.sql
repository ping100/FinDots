-- Категории остались наклейкой: завести можно, толку ноль — ни отбора, ни
-- отдельного вида. Лишняя сущность, которую надо заводить и поддерживать,
-- хуже её отсутствия. Данных в них не было: одна архивная запись и ни одной
-- задачи со ссылкой.
alter table public.tasks drop column category_id;
drop table public.task_categories;
